meta {
  name: write requires key - delete competitor
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/competitors/{{competitorId}}
  body: none
  auth: none
}

assert {
  res.status: eq 401
  res.body.error: eq Unauthorized
}
